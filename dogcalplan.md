# 犬の介護食管理Webアプリ（PWA）全体仕様書

| 項目 | 内容 |
|---|---|
| 文書名 | 犬の介護食管理Webアプリ（PWA）全体仕様書 |
| 版 | 1.2 |
| 更新日 | 2026年8月29日 |
| 対象 | 家族同期版 |
| 文書状態 | 実装開始可能。未確定事項は第18章に分離 |

## 1. 目的

本アプリは、介護中の犬について、その日に摂取したカロリーと管理対象水分量をスマートフォンで簡単に記録し、実績に応じて今後の給餌予定を自動で再計算するためのWebアプリである。

最重要目的は、**管理対象水分量を1日200 ml以下に保つための判断を支援すること**である。その制約内で1日198 kcal以上を目指す。ただし、固定量のバランスリキッドしか与えられず、水分上限との両立が不可能な場合は、カロリー不足を許容する。

本アプリは医療判断を代替しない。栄養値、投薬回数、水分上限その他の設定は、獣医師等の指示を確認した利用者が入力・変更する。アプリは記録と計算の支援を行う。

## 2. 対象範囲

### 2.1 初期版に含む機能

- 当日のカロリーと管理対象水分量の集計
- 6:00〜22:00の給餌予定表示
- バランスリキッド、普通の水、固形食、鶏のスープごはん、ボミットバスター、スープ缶シリンジの実績記録
- 鶏のスープごはん等の実績入力後の未来予定再計算
- 予定と実績の分離管理
- 水分上限、カロリー不足、未投薬、予定遅延等の警告
- 入力の編集、取消し、直前操作の取り消し
- 日別履歴、日次サマリー、メモ
- オフライン動作、端末内保存、PWAとしてのホーム画面追加
- Supabase匿名認証と招待URLによる家族間同期
- 計算条件を変更できる設定画面

### 2.2 初期版に含まない機能

- メールアドレスやパスワードを使う個別アカウント、細かな権限管理
- 獣医師とのオンライン連携
- 自動的な医療判断や給餌の物理的制御
- App Store／Google Playで配布するネイティブアプリ

これらは第16章の将来拡張とする。家族同期は、招待URLを知っている人だけが匿名で参加する簡易方式とする。

## 3. 用語

| 用語 | 定義 |
|---|---|
| 管理日 | 端末の設定タイムゾーンにおける0:00〜23:59の1日。初期値はAsia/Tokyo |
| 管理対象水分量 | 1日200 ml上限の計算に含める水分量 |
| バランスリキッド | 1回18 ml。24 kcal、管理対象水分18 ml |
| 普通の水 | 飲水量だけを入力し、同量を管理対象水分として扱う。カロリー0 kcal |
| 固形食 | カロリーだけを入力する。管理対象水分0 ml |
| 鶏ごはん | 鶏のスープごはん1食。39.9 kcal。管理対象水分は0 mlとして扱う |
| 薬 | ボミットバスター。1回5 ml、1日2回。合計10 mlをあらかじめ水分枠として予約する |
| スープ缶 | スープ缶シリンジ1回。4 kcal、管理対象水分10 ml |
| 通常枠 | 6:00、8:00、10:00、12:00、14:00、16:00、18:00、20:00の8枠 |
| 調整枠 | 22:00の1枠。通常は空け、未達や失敗時の補完に使う |
| 予定 | アプリが未来の枠に提示する推奨。摂取実績には算入しない |
| 実績 | 実際に与えた、または自主的に食べた記録。集計に算入する |
| 予約水分 | 当日まだ記録されていない残りの薬に必要な水分 |

## 4. 確定した栄養条件

### 4.1 1日の目標と優先順位

| 項目 | 値 | 扱い |
|---|---:|---|
| カロリー目標 | 198 kcal | 到達を目指す。超過可 |
| 管理対象水分上限 | 200 ml | 最優先。推奨計画では超過不可 |
| 薬の回数 | 2回／日 | 各5 ml、合計10 mlを予約 |

計算と推奨の優先順位は次のとおりとする。

1. 将来の未投与薬分も含め、管理対象水分200 mlを超える予定を作らない。
2. ボミットバスター2回分の水分枠を確保する。
3. 制約内で198 kcal以上に到達させる。
4. バランスリキッドは必ず1回単位とし、分割量を提案しない。
5. 目標に届かない場合は、水分制約内で得られる最大カロリーを採用する。
6. すでに記録した過去の実績は変えず、未来の予定のみ再計算する。

### 4.2 各項目の標準値

| 種別 | 1回の内容 | kcal | 管理対象水分 | 入力方法 | 自動推奨 |
|---|---|---:|---:|---|---|
| バランスリキッド | 18 ml | 24 | 18 ml | 予定カードまたはクイックボタン | する |
| 普通の水 | 入力したml | 0 | 入力したml | 飲水量だけ入力 | しない |
| 固形食 | 入力したkcal | 入力値 | 0 ml | カロリーだけ入力 | しない |
| 鶏のスープごはん | 1食 | 39.9 | 0 ml | 自主的に食べた時に記録 | しない |
| ボミットバスター | 5 ml | 0 | 5 ml | 1日2回を個別に記録 | 給餌としては推奨しないが水分を予約 |
| スープ缶シリンジ | 1回 | 4 | 10 ml | 実際に与えた時に記録 | しない |

注記：ボミットバスターのカロリーは計算上0 kcalとする。鶏ごはんの表示は利用者向けに「約40 kcal」としてよいが、内部計算には39.9 kcalを使用する。

### 4.3 バランスリキッドの不可分性

- バランスリキッドは1回18 mlの固定量である。追加の水は含めない。
- 4分の1回、半量、任意ml等をアプリから推奨しない。
- 利用者が「一部しか与えられなかった」と記録する機能は初期版では設けず、「失敗／未完了」として扱う。部分量記録は将来拡張とする。

## 5. 基本給餌スケジュール

### 5.1 時刻と役割

| 時刻 | 初期状態 | 役割 |
|---:|---|---|
| 6:00 | バランスリキッド予定 | 通常枠 |
| 8:00 | バランスリキッド予定 | 通常枠 |
| 10:00 | バランスリキッド予定 | 通常枠 |
| 12:00 | バランスリキッド予定 | 通常枠 |
| 14:00 | バランスリキッド予定 | 通常枠 |
| 16:00 | バランスリキッド予定 | 通常枠 |
| 18:00 | バランスリキッド予定 | 通常枠 |
| 20:00 | バランスリキッド予定 | 通常枠 |
| 22:00 | 調整用・原則空き | 失敗、スキップ、予定変更の補完 |

鶏ごはん、固形食、普通の水、スープ缶が0回の場合、バランスリキッド9回と薬2回で、216 kcal／172 mlとなる。水を別に記録した場合は、その量を含めて必要回数を再計算する。

### 5.2 22時調整枠

- 22:00は9回目を常に与える枠ではない。
- 20:00以前のバランスリキッドだけでは目標へ届かず、追加しても予約水分を含め200 ml以下の場合に使用する。
- 再計算時、20:00までの枠だけで必要数を配置できる場合は22:00を空ける。
- 22:00に追加すると水分上限を超える場合は追加を提案しない。
- 普通の水等により9回目を置けない場合は、水分上限を理由として表示する。

### 5.3 薬の時刻

ボミットバスターは1日2回、各5 mlとし、予定時刻は**6:00と12:00に固定**する。タイムラインでは同時刻の薬予定を独立して表示し、実績をそれぞれの予定へ紐づける。予定時刻前であっても事実の記録は可能とする。未投与分の水分予約は常に行う。

## 6. 日中の運用フロー

### 6.1 管理日の開始

1. 当日最初の起動時に新しい管理日を作成する。
2. 前日のデータを削除せず履歴に残す。
3. 当日の設定スナップショットを保存する。
4. 薬2回分10 mlを予約する。
5. 6:00〜20:00の8枠と22:00調整枠から、必要かつ安全なバランスリキッド予定を作る。

### 6.2 バランスリキッドを与えた

1. 利用者が対象予定の「与えた」を押す。
2. クイックボタンと予定カードは同じ処理を使い、24 kcal／18 mlを現在時刻で即時記録する。
3. 記録時刻と予定枠を実績として保存する。
4. 集計を更新する。
5. 未完了の未来予定を再計算する。

### 6.3 鶏ごはんを食べた

1. 利用者が大きな「鶏ごはんを食べた」ボタンを押す。
2. 39.9 kcal／管理対象水分0 mlの実績を現在時刻で保存する。
3. 過去のバランスリキッド実績は変更しない。
4. 現在の累積値から必要なバランスリキッド回数を再計算する。
5. 未来のバランスリキッド予定だけを減らし、できるだけ均等に再配置する。

例：10:00までにバランスリキッド3回を完了し、11:00に鶏ごはん1食を記録した場合、当日の必要合計は7回相当となるため、未来には4回を配置する。さらに鶏ごはんをもう1食記録した場合、合計5回相当となり、未来には2回を配置する。

### 6.4 普通の水・固形食

- 普通の水は飲水量（ml）だけを入力し、現在時刻で即時記録する。カロリーは0とする。
- 固形食はカロリー（kcal）だけを入力し、現在時刻で即時記録する。管理対象水分は0とする。
- どちらも記録後に未来のバランスリキッド予定を再計算する。

### 6.5 ボミットバスターを与えた

1. 「薬5 ml」を押し、1回分を記録する。
2. 6:00または12:00の未完了予定へ実績を紐づける。予定カードから入力した場合はその時刻、クイック入力の場合は早い未完了予定を対象とする。
3. 実績水分を5 ml増やす。
4. 残り予約回数を1減らすため、「実績水分＋予約水分」の合計は原則変わらない。
5. 同じ予定への重複入力または1日2回を超える入力には重複警告を出す。実際に投与済みなら確認後に記録可能とする。

### 6.6 スープ缶シリンジを与えた

1. 実際に与えた場合だけ、4 kcal／10 mlの追加イベントとして記録する。
2. カロリー確保のためにアプリから自動推奨しない。
3. 記録後、予約薬水分を含めて未来予定を再計算する。
4. 記録前に200 ml超過が見込まれる場合は強い警告を表示する。ただし、すでに与えた事実の記録を妨げないよう、確認後は実績として保存できる。

### 6.7 スキップ・失敗

- 予定カードに「スキップ」と「失敗／飲ませられなかった」を用意する。
- どちらも摂取量には加算しない。
- 過去の枠の状態と理由を保存し、未来予定を再計算する。
- 条件を満たす場合は22:00調整枠へ振り替える。

### 6.8 編集・取消し

- バランスリキッド等の実績は内容、時刻、メモを編集できる。普通の水は飲水量、固形食はカロリーだけを編集できる。
- 誤入力は取消しできるが、完全削除ではなく取消し状態と更新日時を保持する。
- 編集・取消し後も同じ再計算処理を実行する。
- クイック入力直後は「元に戻す」を5〜10秒表示する。

## 7. 計算仕様

### 7.1 基本集計

取消されていない当日実績を対象に、次を計算する。

```text
actualCalories = Σ event.caloriesKcal
actualWater = Σ event.countedWaterMl
completedMedicineDoses = count(event.type == VOMIT_BUSTER)
remainingMedicineDoses = max(0, 2 - completedMedicineDoses)
reservedMedicineWater = remainingMedicineDoses × 5
projectedCommittedWater = actualWater + reservedMedicineWater
remainingWaterCapacity = 200 - projectedCommittedWater
remainingCalories = max(0, 198 - actualCalories)
```

画面上の「現在の水分」はactualWater、「薬を含む見込み水分」はprojectedCommittedWaterとして明確に区別する。通常の上限判定にはprojectedCommittedWaterを使う。

### 7.2 必要バランスリキッド回数

```text
dosesNeededForCalories = ceil(remainingCalories / 24)
dosesAllowedByWater = max(0, floor(remainingWaterCapacity / 18))
dosesAllowedBySlots = 未来に利用可能な通常枠数 + 利用可能な22時調整枠数

recommendedRemainingDoses = min(
  dosesNeededForCalories,
  dosesAllowedByWater,
  dosesAllowedBySlots
)
```

カロリー目標到達に必要な数を水分または枠数の制約で置けない場合は、recommendedRemainingDosesを配置した上で「目標到達不可」の理由を表示する。バランスリキッド以外の食品を自動的に補充案として提案しない。

### 7.3 目標到達可否

```text
predictedCalories = actualCalories + recommendedRemainingDoses × 24
predictedWater = actualWater
                 + reservedMedicineWater
                 + recommendedRemainingDoses × 18

calorieReachable = predictedCalories >= 198
waterSafe = predictedWater <= 200
```

waterSafeは推奨計画において常にtrueでなければならない。calorieReachableがfalseでも、水分制約により追加できない場合は正常な結果として扱う。

### 7.4 鶏ごはん回数別の基準結果

スープ缶なし、薬2回、過去の余分な実績なしの場合の基準は次のとおりである。この表をハードコードせず、第7.1〜7.3節の式から算出する。

| 鶏ごはん | バランスリキッド合計 | 合計kcal | 合計管理水分 |
|---:|---:|---:|---:|
| 0食 | 9回 | 216.0 | 172 ml |
| 1食 | 7回 | 207.9 | 136 ml |
| 2食 | 5回 | 199.8 | 100 ml |
| 3食 | 4回 | 215.7 | 82 ml |
| 4食 | 2回 | 207.6 | 46 ml |
| 5食 | 0回 | 199.5 | 10 ml |

### 7.5 未来予定の配置

再計算対象は、未完了かつ未来の枠だけとする。完了済み、スキップ済み、失敗済みの過去枠は変更しない。

配置手順は次のとおりとする。

1. 現在時刻以降に残る6:00〜20:00の通常枠を昇順に取得する。
2. recommendedRemainingDosesが通常枠数以下なら、22:00を使わない。
3. 通常枠を必要数と同じ数の連続グループにできるだけ均等に分割し、各グループの末尾の枠を選ぶ。余り枠は前方のグループに割り当てる。これにより少数のセットを日中に分散しつつ、やや後ろ寄りに配置する。
4. 必要数が通常枠数を超える場合だけ、残る通常枠をすべて選び、22:00を追加する。
5. 予定変更理由として「鶏ごはん摂取」「バランスリキッド完了」「飲水記録」「固形食摂取」「スキップ」「失敗」「スープ缶記録」「実績編集」等を保持する。

例：14:00、16:00、18:00、20:00が残っている場合、必要数2なら16:00と20:00、必要数1なら20:00、必要数4なら全枠に配置する。必要数5で22:00が利用可能なら全通常枠と22:00を使う。

現在時刻を過ぎた未処理枠は「遅延」と表示し、未来の自動配置対象から外す。ただし、そのカードから遅れて実績を記録することはできる。

### 7.6 再計算の発火条件

以下の操作後に同一の再計算処理を実行する。

- バランスリキッドの完了、スキップ、失敗
- 普通の水、固形食、鶏ごはん、薬、スープ缶の記録
- 実績の編集、取消し、復元
- 日次設定の変更
- 管理日の切替え
- アプリ再起動時のデータ復元

## 8. 警告・案内仕様

### 8.1 重要度

| レベル | 表示 | 用途 |
|---|---|---|
| 情報 | 青または中立色 | 正常な不足許容、予定変更、操作結果 |
| 注意 | 黄 | 遅延、目標到達困難、未投薬、重複の可能性 |
| 重大 | 赤 | 水分上限超過、データ不整合、保存失敗 |

色だけに依存せず、アイコン、見出し、文章を併用する。

### 8.2 水分関連

- 追加操作後の`actualWater + reservedMedicineWater`が200 mlを超える場合、確定前に重大警告を出す。
- 未来の推奨作成では、200 mlを超えるバランスリキッドを絶対に配置しない。
- 予約薬水分を除いた単純な残量と、薬を含めた安全残量を混同しない。
- すでに与えたものを記録する場合は、警告後も「実績として記録」を選べる。記録を拒否して履歴を不正確にしない。
- 薬自体の記録で200 mlを超える場合、アプリは薬の中止を指示せず、「水分上限を超えます。対応は獣医師の指示を確認してください」と表示する。

例文：

> このバランスリキッドを追加すると、未投与の薬5 mlを含む見込み水分が203 mlになります。200 mlを超えるため、今後の予定には追加できません。

### 8.3 カロリー関連

- 目標に達した場合：「本日のカロリー目標に到達しました」。
- 目標未達でも水分のため追加不可の場合：情報表示とし、異常扱いにしない。
- 最終的な表示例：「192 / 198 kcal。目標まで6 kcalですが、バランスリキッドを追加すると水分上限を超えるため、本日は追加しません」。
- 残り枠不足で目標に届かない場合：「残り時間枠では目標に到達できません」。

### 8.4 投薬・時間・入力関連

- 6:00または12:00の薬予定を過ぎても該当実績がない場合、未投薬の予定時刻と回数を注意表示する。具体的な投与可否は利用者が判断する。
- 2回を超える薬入力、短時間内の同種イベント連続入力には重複確認を出す。
- 予定時刻を過ぎた未処理枠は「遅延」とする。
- 保存容量不足、端末時計の大幅変更、データ読込み失敗は重大警告とし、データのエクスポートを案内する。

## 9. 画面仕様

### 9.1 共通方針

- スマートフォン縦画面を最優先する。
- 主要操作は片手で押せる大きさとし、タップ領域は最低44×44 CSS pxとする。
- ホーム画面の主要数値と次回予定は、起動後スクロールせず確認できるようにする。
- kcalとml、実績と見込み、予定と完了を明確に分ける。
- 誤操作を防ぐため、記録後に結果と取消し手段を表示する。

### 9.2 今日画面（ホーム）

表示項目：

- 日付、犬の名前（設定時）
- カロリー：`実績 / 198 kcal`、残りまたは超過
- 水分：`実績 / 200 ml`
- 薬を含む見込み水分、安全残量
- 鶏ごはん回数
- バランスリキッド完了数／本日の再計算後必要合計
- 次回予定の時刻と内容
- 目標到達可否と現在の警告
- 6:00〜22:00の予定・実績タイムライン

主要ボタン：

- 「バランスリキッドを与えた」（1タップで現在時刻に記録）
- 「普通の水を飲んだ」（飲水量だけ入力）
- 「固形食を食べた」（カロリーだけ入力）
- 「鶏ごはんを食べた」
- 「薬5 mlを与えた」
- 「スープ缶シリンジを与えた」

画面表示例：

```text
今日 8/28

カロリー       216 / 198 kcal
目標超過       18 kcal

実績水分       162 / 200 ml
薬を含む見込み 172 / 200 ml
安全残量       28 ml

鶏ごはん       0食
バランスリキッド 9回完了

次回 —         追加不要
本日のカロリー目標に到達しました
```

### 9.3 予定・タイムライン

各枠には次を表示する。

- 予定時刻
- 予定内容と量
- 状態：予定、完了、不要、スキップ、失敗、遅延、調整待ち
- 実際の記録時刻
- 「与えた」「スキップ」「失敗」操作
- 予定が変わった場合の短い理由

予定外イベント（普通の水、固形食、鶏ごはん、薬、スープ缶）は実時刻順に通常枠の間へ表示する。

### 9.4 入力確認画面／ボトムシート

- バランスリキッドは確認画面を挟まず、予定カードとクイックボタンのどちらからでも同じ24 kcal／18 mlを現在時刻で記録する。
- 普通の水は飲水量、固形食はカロリーだけの入力画面を表示する。
- その他は種別、標準量、kcal、水分、記録時刻を表示する。
- 通常操作では標準値を直接編集させない。
- 記録時刻とメモは変更可能にする。
- 操作後の予測合計と警告を表示する。
- 重大警告時は「戻る」と「すでに与えたので実績として記録」を区別する。

### 9.5 履歴画面

- 日付一覧またはカレンダーを表示する。
- 各日に、総kcal、総水分、薬回数、鶏ごはん回数、バランスリキッド回数、警告有無を表示する。
- 日を選ぶと、その日の設定スナップショット、全イベント、予定変更、メモを閲覧できる。
- 過去日編集時は「過去の記録を編集しています」と明示する。

### 9.6 設定画面

- 犬の名前
- タイムゾーン
- カロリー目標、水分上限
- 各食品・薬の名称、kcal、水分、標準量
- 薬の名称と1回量（1日2回、6:00・12:00は固定値として表示）
- 通常枠と調整枠の時刻
- 通知設定（将来または対応ブラウザのみ）
- データのエクスポート／インポート／全データ消去

栄養条件変更時は、「今日だけ」「明日以降」のどちらに適用するか確認する。過去日の設定スナップショットは変更しない。

## 10. 状態とデータ構造

### 10.1 設計原則

- 金額と同様に栄養値の誤差を避けるため、小数は浮動小数の表示誤差が出ないよう整数化して保持する。例：kcalは0.1 kcal単位（39.9 kcal → 399）で保存する。
- 予定は実績に加算しない。
- 集計値は原則イベントから再計算し、二重管理を避ける。
- 管理日ごとに設定スナップショットを保存し、後日の設定変更で過去結果が変わらないようにする。
- 全レコードに一意ID、作成日時、更新日時を持たせる。

### 10.2 設定

```ts
type AppSettings = {
  schemaVersion: number;
  dogName?: string;
  timezone: string;                 // default: "Asia/Tokyo"
  calorieTargetTenthKcal: number;   // 1980
  waterLimitMl: number;             // 200
  foods: {
    balanceLiquid: {
      name: "バランスリキッド";
      amountMl: 18;
      caloriesTenthKcal: 240;
      countedWaterMl: 18;
      indivisible: true;
    };
    chickenMeal: {
      servingLabel: string;
      caloriesTenthKcal: 399;
      countedWaterMl: 0;
    };
    soupSyringe: {
      caloriesTenthKcal: 40;
      countedWaterMl: 10;
    };
  };
  medicine: {
    name: string;                   // ボミットバスター
    doseMl: 5;
    dosesPerDay: 2;
    caloriesTenthKcal: 0;
    scheduledTimes: ["06:00", "12:00"];
  };
  regularSlotTimes: string[];       // 06:00 ... 20:00
  adjustmentSlotTime: string;       // 22:00
};
```

### 10.3 管理日

```ts
type DayRecord = {
  id: string;
  localDate: string;                // YYYY-MM-DD
  timezone: string;
  settingsSnapshot: AppSettings;
  note?: string;
  createdAt: string;
  updatedAt: string;
};
```

### 10.4 実績イベント

```ts
type IntakeEventType =
  | "BALANCE_LIQUID"
  | "PLAIN_WATER"
  | "SOLID_FOOD"
  | "CHICKEN_MEAL"
  | "VOMIT_BUSTER"
  | "SOUP_SYRINGE";

type IntakeEvent = {
  id: string;
  dayId: string;
  type: IntakeEventType;
  occurredAt: string;
  linkedSlotId?: string;
  medicineScheduledTime?: "06:00" | "12:00";
  caloriesTenthKcal: number;
  countedWaterMl: number;
  quantity: number;                 // 初期版は常に1
  note?: string;
  status: "ACTIVE" | "VOIDED";
  voidReason?: string;
  createdAt: string;
  updatedAt: string;
};
```

イベントには記録時点の栄養値をコピーする。後で設定が変更されても既存イベントの値は変えない。

### 10.5 スケジュール枠

```ts
type SlotStatus =
  | "PLANNED"
  | "COMPLETED"
  | "NOT_REQUIRED"
  | "SKIPPED"
  | "FAILED"
  | "OVERDUE"
  | "ADJUSTMENT_AVAILABLE";

type ScheduleSlot = {
  id: string;
  dayId: string;
  scheduledTime: string;
  role: "REGULAR" | "ADJUSTMENT";
  status: SlotStatus;
  plannedType?: "BALANCE_LIQUID";
  linkedEventId?: string;
  changeReason?: string;
  revision: number;
  updatedAt: string;
};
```

### 10.6 派生サマリー

サマリーはイベントと設定から都度算出する。

```ts
type DailySummary = {
  actualCaloriesTenthKcal: number;
  actualWaterMl: number;
  reservedMedicineWaterMl: number;
  projectedCommittedWaterMl: number;
  safeRemainingWaterMl: number;
  completedMedicineDoses: number;
  chickenMealCount: number;
  completedBalanceLiquidDoses: number;
  recommendedRemainingDoses: number;
  predictedCaloriesTenthKcal: number;
  predictedWaterMl: number;
  calorieReachable: boolean;
  waterSafe: boolean;
  warnings: Warning[];
};
```

## 11. 履歴・監査・データ保護

- 当日の予定変更履歴を保持し、「いつ、何が原因で、どの未来枠が変わったか」を確認できるようにする。
- 実績の取消しは論理取消しとする。
- 初期版では操作者名を持たない。匿名の家族同期では端末ごとの表示名も求めない。
- JSONエクスポートを完全バックアップ形式とする。
- CSVエクスポートは日次サマリーとイベント明細の2種類を用意する。
- インポート前に形式とschemaVersionを検証し、既存データの置換／統合を確認する。
- 「全データ消去」は確認文入力等の二段階確認を行う。

## 12. PWA・保存方式

### 12.1 初期構成

- レスポンシブWebアプリ
- Web App Manifest（名称、アイコン、テーマ色、standalone表示）
- Service Workerによるアプリ本体のオフラインキャッシュ
- IndexedDBによるイベント、設定、履歴の端末内保存
- Supabase Postgres／Realtimeによる家族データ同期
- Supabase匿名認証と長い招待トークンによる家族参加
- 初回読込み後は通信なしで主要機能を利用可能

### 12.2 オフライン時の要件

- 記録、編集、取消し、集計、再計算、履歴閲覧が動作する。
- オフライン時は端末へ記録し、「同期待ち」と表示して再接続後に自動送信する。
- アプリ更新で既存データを失わない。
- データ移行に失敗した場合は旧データを保持し、エクスポート可能な復旧画面を出す。

## 13. 非機能要件

### 13.1 性能

- 通常の入力から集計・画面更新まで300 ms以内を目標とする。
- 当日画面の初期表示は一般的なスマートフォンで1秒以内を目標とする（アプリ本体がキャッシュ済みの場合）。
- 3年分の日次記録を保持しても日常操作が著しく遅くならないこと。

### 13.2 信頼性

- 同じ操作の二重タップを抑止する。
- イベント保存と予定再計算は、途中状態が見えないよう一つの処理単位として扱う。
- 再起動後、イベントからサマリーと予定を再構築できる。
- 端末時計やタイムゾーン変更時に管理日の所属を明示し、勝手に過去記録を移動しない。

### 13.3 使いやすさ・アクセシビリティ

- 主要操作は最大2タップで完了する。
- 本文16 px以上を基本とし、OSの文字拡大に追従する。
- コントラストはWCAG 2.2 AAを目標とする。
- キーボード操作、スクリーンリーダー用ラベル、フォーカス表示に対応する。
- 状態を色だけで区別しない。

### 13.4 セキュリティ・プライバシー

- 介護記録は家族同期のためSupabaseへ保存する。
- 招待URLを知っている人は家族データへ参加できるため、公開IssueやSNSには掲載しない。
- 招待トークンはDBへハッシュで保存し、RLSにより参加済み家族のデータだけを読み書き可能にする。
- メールアドレスやパスワードの入力は求めず、匿名セッションを端末へ保存する。
- 解析ツールを導入する場合、個別の摂取内容やメモを送信しない。
- HTMLへメモを表示する際はエスケープし、スクリプト注入を防止する。
- 依存ライブラリを定期更新し、既知の脆弱性を確認する。

### 13.5 対応環境

- iPhone Safariの現行主要版
- Android Chromeの現行主要版
- PC版Chrome／Edgeの現行主要版
- PWAのインストール方法が環境により異なるため、初回案内をOS別に表示する。

## 14. エラー処理

- 保存失敗時は入力内容を画面に保持し、再試行できるようにする。
- 不正な負数、NaN、上限外の設定値を保存しない。
- IndexedDBが利用できない場合は、閲覧のみまたは一時セッションであることを明示する。
- 予期しない計算エラー時は、直前の安全な予定を表示し、「計算できないため手動で確認」と重大表示する。
- 計算ロジックの例外時に、水分上限を満たすと断定しない。

## 15. 受入れテストケース

以下の期待値は、特記がない限り薬2回分10 mlを含む。

| ID | 条件／操作 | 期待結果 |
|---|---|---|
| T01 | 新しい日を開始 | バランスリキッド9回（22時を含む）。見込み216 kcal／薬込み172 ml |
| T02 | 鶏ごはん0食、バランスリキッド9回、薬2回を完了 | 実績216 kcal／172 ml。カロリー目標到達 |
| T03 | 普通の水50 mlを記録 | 水分だけ+50 ml。安全なバランスリキッド予定を7回へ再計算 |
| T04 | 鶏ごはん1食を記録 | 必要バランスリキッド合計7回。見込み207.9 kcal／136 ml |
| T05 | 鶏ごはん2食を記録 | 必要バランスリキッド合計5回。見込み199.8 kcal／100 ml |
| T06 | 鶏ごはん3食を記録 | 必要バランスリキッド合計4回。見込み215.7 kcal／82 ml |
| T07 | 鶏ごはん4食を記録 | 必要バランスリキッド合計2回。見込み207.6 kcal／46 ml |
| T08 | 鶏ごはん5食を記録 | バランスリキッド0回。見込み199.5 kcal／10 ml |
| T09 | バランスリキッド3回後、鶏ごはん1食 | 過去3回は維持し、未来4回を配置。合計7回相当 |
| T10 | T09後、鶏ごはんをさらに1食 | 過去3回は維持し、未来2回を配置。合計5回相当 |
| T11 | 14時時点で通常枠14/16/18/20、必要2回 | 16時と20時に配置し、22時は空ける |
| T12 | 残る通常枠が1つ、必要2回、水分に余裕 | 通常枠と22時に1回ずつ配置 |
| T13 | バランスリキッドをスキップ | 摂取量は増えず、未来を再計算。必要なら22時を使用 |
| T14 | バランスリキッドが失敗 | 履歴に失敗を残し、未来へ振替え。過去枠は書換えない |
| T15 | 薬1回を記録 | 実績水分+5、予約水分-5、見込み水分は不変 |
| T16 | 薬2回を記録後、3回目を押す | 重複警告。確認なしでは保存しない |
| T17 | 水分178 ml、薬1回未投与でバランスリキッドを追加しようとする | 見込み201 mlとなるため重大警告、予定には追加しない |
| T18 | 水分上限を超えるスープ缶を「すでに与えた」と記録 | 重大警告後に実績保存可能。超過状態を常時表示 |
| T19 | スープ缶1回を記録 | +4 kcal、+10 mlし、未来のバランスリキッドを再計算。スープ缶は自動推奨しない |
| T20 | 鶏ごはん記録を取消し | カロリーを戻し、未来のバランスリキッドを再計算 |
| T21 | バランスリキッドを素早く二重タップ | 1件だけ記録する |
| T22 | アプリをオフラインで再起動 | 当日実績・予定・履歴を復元し、入力と再計算が可能 |
| T23 | 日付が変わる | 新しい管理日を作成し、前日データは履歴に残る |
| T24 | 設定を明日以降に変更 | 過去日と当日の設定スナップショットは変わらない |
| T25 | 12時を過ぎても薬が1回も記録されていない | 「6:00・12:00の薬が2回未記録」の注意表示 |
| T26 | 過去時刻の未処理予定 | 遅延表示。未来の自動配置対象外だが、遅れて実績入力可能 |
| T27 | JSONバックアップを出力し再取込み | イベント、取消し状態、設定スナップショット、メモが一致 |
| T28 | 0.1 kcalを含む複数イベントを集計 | 浮動小数の表示誤差がなく、39.9等を正確に表示 |
| T29 | 固形食100 kcalを記録 | カロリーだけ+100 kcal。未来予定を再計算 |
| T30 | ホーム画面版へ招待URLを貼り付けて参加後に再起動 | 招待URLの再入力なしで同じ家族データへ自動接続 |

### 15.1 計算ロジックの単体テスト

- 境界値：残り水分17 mlではバランスリキッド0回、18 mlでは1回。
- 境界値：残りカロリー0なら0回、0.1〜24.0 kcalなら1回、24.1 kcalなら2回。
- 予約薬水分が0、5、10 mlの各場合を検証する。
- 200 mlちょうどは許容し、200 ml超は推奨不可とする。
- 取消しイベントを集計から除外する。
- 設定スナップショットの異なる日を独立して計算する。

## 16. 将来拡張

優先候補は次のとおりとする。

1. 操作者名と端末管理
2. 招待の無効化・再発行を行う管理画面
3. 給餌・投薬時刻の通知
4. 体重、体温、排泄、嘔吐、体調、写真、自由メモ
5. 週間・月間グラフ、平均、目標達成率
6. 獣医師向けPDF／CSVレポート
7. 食品・薬の追加と複数犬対応
8. 部分量、半食、任意量への対応
9. 自動バックアップと簡単な端末移行
10. 役割別権限、監査ログ、緊急連絡先

家族同期ではサーバーを正とし、更新番号による楽観ロックで同一枠への二重記録を検知する。オフライン入力は一意ID付きで端末へ保存し、再接続時に同期する。競合時に片方を黙って破棄しない。

## 17. 実装上の注意

- 鶏ごはん回数別の表を直接ロジック化せず、イベント合計と制約式から毎回算出する。
- 水分判定には、実績だけでなく未投与薬の予約水分を必ず含める。
- 警告を避けるために実績入力自体を禁止しない。「推奨」と「事実の記録」を区別する。
- 「カロリー不足」は常にエラーではない。水分優先の結果として許容される状態がある。
- 予定の再計算で過去実績を削除・変更しない。
- 日次設定とイベントに栄養値をスナップショット保存し、設定変更による履歴改変を防ぐ。
- 22時は調整枠であり、通常の9回目ではない。
- スープ缶は自動推奨の候補に入れない。
- ボミットバスターの予定時刻は6:00と12:00に固定し、それぞれの未記録状態を区別する。

## 18. 未確定事項・利用開始前の確認

以下は現在の会話では確定していない。初期版は記載の安全な既定動作とし、利用者が設定可能にする。

| 項目 | 初期版の扱い | 確認先 |
|---|---|---|
| ボミットバスターのカロリー | 0 kcalとして計算 | 必要なら製品情報 |
| 鶏ごはん1食未満の記録 | 非対応。1食単位のみ | 運用上必要か確認 |
| バランスリキッドの一部しか入らなかった場合 | 失敗扱い。部分量は記録しない | 記録精度の方針 |
| 日付切替え時刻 | 0:00、Asia/Tokyo | 夜間運用に応じて確認 |
| 通知の要否 | 初期版では任意／ブラウザ依存 | 利用者 |

これらを変更しても、第4章の「水分200 ml最優先」「バランスリキッドは固定量」「過去は変えず未来だけ再計算」という中核原則は維持する。

## 19. 初期版の完了条件

次をすべて満たした時、初期版を受入れ可能とする。

- 第15章の必須テストが合格する。
- 水分200 mlを超える未来予定を生成しない。
- 鶏ごはん入力後、過去を変えずに未来予定が即時更新される。
- 鶏ごはん、普通の水、固形食が0回の日にバランスリキッド9回／216 kcal／薬込み172 mlを正しく扱う。
- 普通の水は飲水量だけ、固形食はカロリーだけを入力し、記録後に未来予定が更新される。
- ホーム画面版で招待URLを一度貼り付けた後は、再入力なしで家族データへ自動接続する。
- 22時を原則空け、必要かつ安全な場合だけ調整枠として使う。
- オフラインで当日の主要操作と履歴閲覧ができる。
- アプリ更新・再起動で保存済みデータが失われない。
- 実績の誤入力を編集・取消しでき、再計算結果が正しい。
- スマートフォンで主要情報を読みやすく、主要操作を2タップ以内で完了できる。

---

以上
