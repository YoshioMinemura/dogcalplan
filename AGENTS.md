# AGENTS.md

このファイルは、べぬケアごはんを開発するエージェント向けのプロジェクト指示である。リポジトリ全体に適用する。

## 開発開始時に最初に見るもの

次の順序で確認する。

1. `AGENTS.md` — 作業規約と終了条件。
2. `docs/dogcalplan_expansion.md` — 最新の変更要求、確定事項、作業状況、未完了事項。
3. `dogcalplan.md` — 現行機能の利用者向け仕様と計算上の不変条件。
4. `docs/implementation-guide.md` — 現在のコード構成、保存・同期・移行・公開方法。
5. `docs/family-sync-plan.md` — Supabase/GitHub Pagesの構築と家族向け運用。
6. `README.md` — 起動方法と主要機能の概要。
7. `git status --short`、`git diff`、直近のgit log — ユーザーの未完了変更を把握する。

その後、要求に関係する実装ファイルとテストを読む。文書だけを根拠にコードの現状を決めつけない。

## 情報の優先順位

矛盾がある場合は、原則として次を優先する。

1. 現在のユーザー指示
2. `docs/dogcalplan_expansion.md` に記録された最新の確定事項
3. `dogcalplan.md` の現行仕様
4. 実コードとテストが示す現在の挙動
5. 実装ガイド、同期手順、READMEの説明

実コードが仕様と食い違う場合は、勝手に仕様を変えず、要求の意図を確認して両方を揃える。

## 守るべき実装上の原則

- 既存の介護記録を失わせない。イベントは原則として物理削除せず、取消し状態を残す。
- 設定とイベントの栄養値スナップショットを維持し、後日の設定変更で過去集計を変えない。
- 保存形式を変える場合はschemaVersionを上げ、ローカル・JSONバックアップ・Supabaseの旧stateを移行できるようにする。
- カロリーは0.1 kcal単位の整数で保持する。
- 管理水分200 mlと未投与薬の予約水分を計算上の最優先にする。
- ボミットバスターは06:00と12:00の2回固定。
- バランスリキッドは標準24 kcal／18 mlで、18 ml自体を水分として数える。
- 普通の水は飲水量だけ、固形食はカロリーだけを記録する。
- クイック操作とタイムライン操作で同じ種類の実績に差を作らない。
- 同期競合を黙って捨てない。二重実績はVOIDED履歴と警告を残す。
- 利用者由来の文字列をHTMLへ入れるときはエスケープする。
- ユーザーの未コミット変更を上書き・削除しない。

## 変更時の確認

- `js/defaults.js` の保存形式を変更したら移行処理と移行テストを追加する。
- 計算を変更したら `js/tests.js` と `dogcalplan.md` の基準値を同時に更新する。
- 実績種別を変更したら、記録、編集、履歴、CSV、同期マージを一通り確認する。
- 実行時ファイルを追加したら、`sw.js` の `APP_SHELL` とGitHub Pages workflowのコピー対象へ追加する。
- JavaScript、CSS、HTML、manifest、アイコンなど公開アプリの内容を変更したら、`sw.js` の `CACHE_NAME` を更新する。
- Supabase schemaやRPCを変更したら、新しいmigrationを追加する。適用済みmigrationを履歴から消したり、安易に書き換えたりしない。
- Project URLとpublishable key以外の秘密情報をリポジトリへ保存しない。招待URLも保存しない。
- 本番Supabaseへの書込み、GitHubへのpush、公開操作は、ユーザーから依頼された範囲でのみ行う。

## テスト

最低限、ローカルHTTPサーバーから `tests.html` を開いて全件合格を確認し、`git diff --check` を実行する。

変更内容に応じて次も確認する。

- 今日画面、履歴、設定の表示
- 記録、編集、取消し、再読込み後の復元
- 水分境界値と薬予約水分
- 旧schemaからの移行
- 2端末同期、オフライン復帰、同時操作
- ホーム画面版の更新と一度限りの招待URL貼り付け

実環境で確認できなかった項目は、完了したように書かず引き継ぎへ明記する。

## スレッド終了時に更新するもの

作業内容に応じて、終了前に次を更新する。

1. `docs/dogcalplan_expansion.md`
   - 今回の要求と確定した判断
   - 実装済み／未実装
   - 実行したテストと結果
   - 次スレッドで行うこと、手動確認事項
2. `dogcalplan.md`
   - 利用者から見える挙動、計算条件、データ仕様が変わった場合
3. `docs/implementation-guide.md`
   - ファイル構成、データフロー、保存、同期、移行、公開方法が変わった場合
4. `docs/family-sync-plan.md`
   - Supabase、招待、公開、家族の運用手順が変わった場合
5. `README.md`
   - 起動方法、主要機能、セットアップ入口が変わった場合
6. `js/tests.js`
   - 新しい仕様と回帰条件をテストとして残す

最後に、変更ファイル、テスト結果、未確認事項、ユーザーが次に行う操作を簡潔に報告する。文書の日付は実際の作業日に合わせ、完了していない項目を完了扱いにしない。
